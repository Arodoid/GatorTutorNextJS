#  Controls npm behavior
#  - Sets official registry
#  - Configures retry logic for failed downloads
#  - Enables offline mode when possible
#  Important because it makes npm installations more reliable in Docker
registry=https://registry.npmjs.org/
fetch-retries=5
fetch-retry-factor=2
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000
prefer-offline=true 